require "test_helper"

class RegistrationsControllerTest < ActionDispatch::IntegrationTest
  setup do
    @supportUser = users(:support)
    @clientUser = users(:client)
  end

  test "should register user and task_monts should be created as many as month left to ceremony" do

  end

end